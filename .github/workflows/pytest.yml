name: pytest

on:
  push:
    branches:
    - main
    - development
  pull_request:
  workflow_dispatch:

# jobs:
#   test:
#     runs-on: ubuntu-latest

#     strategy:
#       matrix:
#         python-version: ["3.9", "3.10", "3.11", "3.12"]

#     # services:
#     #   postgres:
#     #     image: postgis/postgis:17-3.5  # docker image

#     # steps:
#     #   - name: Wait for Postgres to be ready and run initdb
#     #     run: |
#     #       # Wait until the service is ready
#     #       for i in {1..10}; do
#     #         pg_isready -h localhost -p 5432 -U test && break
#     #         echo "Waiting for postgres..."
#     #         sleep 2
#     #       done

#       - uses: actions/checkout@v4

#       - name: Set up Python ${{ matrix.python-version }}
#         uses: actions/setup-python@v5
#         with:
#           python-version: ${{ matrix.python-version }}

#       - name: Install GeoGridFusion
#         run: |
#           python -m pip install --progress-bar off --upgrade pip
#           pip install .[test]

#       - name: Test with pytest 
#         run: |
#           python -m pytest --cov=./ --cov-report=xml


jobs:
  test:
    runs-on: ubuntu-latest

    strategy:
      matrix:
        python-version: ["3.9", "3.10", "3.11", "3.12"]

    steps:
      - name: Set up PostgreSQL with PostGIS
        run: |
          sudo apt-get update
          sudo apt install gnupg2 wget
          sudo sh -c 'echo "deb http://apt.postgresql.org/pub/repos/apt $(lsb_release -cs)-pgdg main" > /etc/apt/sources.list.d/pgdg.list'
          curl -fsSL https://www.postgresql.org/media/keys/ACCC4CF8.asc | sudo gpg --dearmor -o /etc/apt/trusted.gpg.d/postgresql.gpg
          sudo apt update
          sudo apt install postgresql-17 postgresql-contrib-17 postgresql-17-postgis-3
      - name: Verify postgres is available
        run: |
          which postgres
          postgres --version
          which initdb
          initdb --version
                
      - uses: actions/checkout@v4

      - name: Set up Python ${{ matrix.python-version }}
        uses: actions/setup-python@v5
        with:
          python-version: ${{ matrix.python-version }}

      - name: Install GeoGridFusion
        run: |
          python -m pip install --progress-bar off --upgrade pip
          pip install .[test]

      - name: Test with pytest
        env:
          PGHOST: localhost
          PGPORT: 5432
          PGUSER: test
          PGPASSWORD: test
          PGDATABASE: test_db
        run: |
          python -m pytest --cov=./ --cov-report=xml